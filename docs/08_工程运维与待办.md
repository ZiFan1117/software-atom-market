# 工程运维与待办（状态跟踪）

> 本文件只放**发布/收录/凭证/验证**这类工程运维进度；研究/设计层面的开放问题在 `docs/07`。
> 时间：2026-09-06，状态：持续更新。

## 仓库布局

- 商店：`ZiFan1117/software-atom-market`（本地 `D:\Ontology\code-module-store`）——spec + atoms + 收录 + docs。
- 插件：`ZiFan1117/dsh-atom-market`（本地 `D:\dsh-atom-market`）——DSH 社区插件，`dsh-plugin` topic 已打。

## 发布与安装

- [x] **npm：放弃**（账号受限）。安装统一走 GitHub 直装：`dsh plugin add github:ZiFan1117/dsh-atom-market`（`lib/` 随仓，无需构建）。
- [ ] **桌面真机复核**：当前桌面装的还是"本地指路"旧版；插件 GitHub 直连版（v0.1.1+）只做过 CLI 验证，**未在 DSH Desktop 内实测** `atom_search/read` 走 GitHub 源。→ 待 PR 合并后用 `dsh plugin add github:ZiFan1117/dsh-atom-market` 重装复验。

## 凭证与网络

- [x] **自动化令牌**：classic PAT（环境变量 `GITHUB_PAT`，`repo` scope，过期 2026-11-24）——可自动对他人仓库开 PR/收录。勿提交进 git。
- [x] **git push 网络**：本机直连 github 被间歇 reset；走本机代理 `http://127.0.0.1:7890` 稳定（可用 `git -c http.proxy=... push`，或写进仓库 config 永久化——暂未写）。

## 收录（awesome-dsh-plugin / dsh-market）

- [~] **PR #4460（open）**：https://github.com/awesome-dsh-plugin/awesome-dsh-plugin/pull/4460
  - 已达标并勾选：单文件投稿 / `dsh.bundle` / `category: tools` / 描述无营销词 / 仓库含 `dsh-plugin` topic。
  - **待满足：仓库满 1 天**。`dsh-atom-market` 建于 `2026-09-05T16:44:03Z` → `2026-09-06T16:44:03Z`（北京时间 09-07 00:44）起达标。
  - 到点动作：补勾该清单项 → 等维护者合并。合并后 dsh-market（从 awesome 拉源）自动收录。
  - ⚠ 风险待观察：评审是否接受"github spec 安装"（未发 npm）。

## 常规命令

```sh
# 商店校验 / 推送（走代理）
node D:\Ontology\code-module-store\scripts\validate.mjs
git -C D:\Ontology\code-module-store -c http.proxy=http://127.0.0.1:7890 push origin main

# 插件构建/测试/推送
cd D:\dsh-atom-market && npm run build && node --test
git -C D:\dsh-atom-market -c http.proxy=http://127.0.0.1:7890 push origin main
```
