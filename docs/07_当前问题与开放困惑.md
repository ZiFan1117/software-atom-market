# 当前问题与开放困惑（对话沉淀）

> 目的：把一路讨论中**没解决/待定/会再想**的问题钉下来，让下一次（人或 Agent）能直接从这些断点接续。
> 时间：2026-09，状态：持续更新。

## 0. 当前已定与已完成（简要基线）

- 商店（spec+atoms+收录）与 DSH 插件**已拆成两个仓**（对齐 DSH 社区插件方式）；插件打上 `dsh-plugin` 等 topics。
- 插件 v0.1.x：默认数据源 = GitHub 商店（零配置），`atom_search/read/validate/draft` 四工具；`atom_validate/draft` 已在桌面验证，CLI 验证远程读 4 原子。
- 契约 v0.1 + 渐进披露（`intent` 一句话 / `description` 四节详情 `spec/detail-convention.md`）。
- 对外叙事 `docs/00`（8 条主张）。研究笔记 `docs/06`（本体/图纸层）。

## 1. 外部阻塞（需要你的账号/授权，非技术问题）

- [x] **npm 发布：放弃（账号受限）。** 安装统一走 GitHub 直装 `dsh plugin add github:ZiFan1117/dsh-atom-market`（`lib/` 随仓，无需构建）。
      ⚠ 风险待观察：dsh-market / awesome-dsh-plugin 收录评审是否接受 github spec 安装（PR 反馈见下）。
- [x] **自动化令牌已配**：classic PAT（环境变量 `GITHUB_PAT`，`repo` scope）→ 跨仓收录/PR 可全自动，不再需要浏览器手动点。
- [~] **awesome/dsh-market 收录 PR #4460**（open）：https://github.com/awesome-dsh-plugin/awesome-dsh-plugin/pull/4460
      清单项已勾（单文件 / `dsh.bundle` / `tools` 分类 / 无营销词 / `dsh-plugin` topic）；唯一待满足 = **仓库满 1 天**（`dsh-atom-market` 建于 2026-09-05T16:44Z → 2026-09-06T16:44Z 起达标，届时补勾待合并）。合并后自动进 dsh-market（其从 awesome 拉源）。
- [ ] **桌面真机复核**：当前桌面装的是"本地指路"版；插件默认 GitHub 直连版（v0.1.1+）只做了 CLI 验证，**还没在 DSH Desktop 内实测** `atom_search/read` 走 GitHub 源。PR 合并后重装或直接用 `dsh plugin add github:ZiFan1117/dsh-atom-market` 复验。

## 2. 设计张力（真矛盾，需给结论）

- [ ] **"只收 manifest + 实现外链" vs "拿来即跑 MVP"**（叙事 #5）。
  中央仓不收实现 → 无法替作者运行验证 → "拿到即最小验证可用、省 90% 算力"靠什么兜底？
  候选：a) `verified=true` 要求作者给"实现仓 CI 上契约测试通过"的自证/徽章；b) 中央托管执行（=逼近商业化托管，偏离轻量）；c) 明确收窄语义："商店验证契约合法性 + 作者自证实现可跑"，不做中央执行。**待拍板，影响 verified 定义。**
- [ ] **90% 目标是口号不是结论**：无指标/基线/实验。别在外宣与论文里当已证事实（见 docs/04）。
- [ ] **检索是 substring**：商店涨大即退化。缺语义 embedding + 检索基准（对应用户的"先检索而非生成"主张，需数据支撑）。

## 3. 结构决策的"尾巴"

- [ ] **商店改名的脆弱性**：插件默认 store 常量 `ZiFan1117/software-atom-market` 硬编码。有 env 覆盖，但没有"商店地址发现/注册"机制（此职责未来属于商店后台/注册表）。
- [ ] **拆仓触发条件清单**（何时该把商店与插件再拆 / 或插件子包独立发版）只在对话里说过，未落盘 → 可补进 `docs/05` 或独立段落。

## 4. 学术/工程未决

- [ ] **本体/图纸层只有笔记（docs/06）**：L1 依赖与数据流图如何推导、值域类型词汇从哪来、schema↔OWL 缝合，全未定。
- [ ] **`description` 四节无机器 lint**：靠维护者人工打回。可加"标题齐全性"软警告。
- [ ] **`verified` 生命周期未定义**：谁复核？升级版本要不要重验？verified 原子如何撤回？
- [ ] **DSH developer preview 破坏性变更**：peer 锁 rc 版本；跨版本维护策略要定期审视。
- [ ] **"创造性在组合/框架层、框架也原子化"是主张未验证**：对应 docs/04 的 H1-H4，先当假说，勿过度宣称。

## 5. 语义口径（对外防混淆）

- 原子/能力原子/实现原子/函数级——已澄清（docs/01），对外表述要用统一词防再次混淆。
- "收录 vs 收入""渐进披露 vs 图纸"——对话里反复出现过歧义，文档里尽量固定用词。

## 6. 建议的下一批最小动作（按性价比排序）

1. 桌面实测 GitHub 默认源四工具闭环（一次会话内可完成，最能闭环当前叙事）。
2. 把"只收 manifest vs 拿到即 MVP"矛盾拍板 → 落成 verified 语义 v0.2。
3. 完成收录（PR + npm）或明确推迟并记录原因。
4. docs/04 的 H3 微型预实验（10~20 原子两档粒度测选砖命中率），给"检索 vs 再生成"首个数据。
5. （可选）L1 依赖图推导 demo：从 `deps` + input/output 生成 Atom 依赖/数据流 JSON-Graph。
