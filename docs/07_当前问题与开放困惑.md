# 当前问题与开放困惑（研究/设计向）

> 目的：钉住讨论里**在研究/设计层面**还没解决、待定、会再想的开放问题，让下一次（人或 Agent）直接从断点接续。
> 运维/工程进度（收录 PR、令牌、发布、本机命令等）是本地流水账，**不进本仓**（仅存本地 `.ops_local.md`），以免与公开仓库无关甚至泄密。
> 时间：2026-09，状态：持续更新。

## 0. 已定的研究基线

- 能力原子 vs 实现原子：颗粒由"用户一次意图"定义（docs/01）。
- 契约 v0.1 + 渐进披露：`intent`（一句话，列表层）/ `description`（四节 Markdown 详情层，`spec/detail-convention.md`）。
- 四层拆分：L0 契约 / L1 组装依赖·数据流 / L2 单原子图纸 / L3 实现（docs/06）。
- 对外叙事：docs/00（8 条主张，其中 90%、创造性位置为待验证主张）。

## 1. 设计张力（真矛盾，需拍板）

- [ ] **"只收 manifest + 实现外链" vs "拿来即跑 MVP"**（叙事 #5）。
  中央仓不收实现 → 无法替作者运行验证 → "拿到即最小验证可用、省 90% 算力"靠什么兜底？
  候选：a) `verified=true` 要求作者给"实现仓 CI 上契约测试通过"的自证/徽章；b) 中央托管执行（=逼近商业化托管，偏离轻量）；c) 明确收窄语义："商店验证契约合法性 + 作者自证实现可跑"，不做中央执行。**待拍板，影响 verified 定义。**
- [ ] **90% 目标是口号不是结论**：无指标/基线/实验。别在外宣与论文里当已证事实（见 docs/04）。
- [ ] **检索是 substring**：商店涨大即退化。缺语义 embedding + 检索基准（对应用户的"先检索而非生成"主张，需数据支撑）。

## 2. 结构决策的"尾巴"

- [ ] **商店改名的脆弱性**：插件默认 store 常量 `ZiFan1117/software-atom-market` 硬编码。有 env 覆盖，但没有"商店地址发现/注册"机制（此职责未来属于商店后台/注册表）。
- [ ] **拆仓触发条件清单**（何时该把商店与插件再拆 / 或插件子包独立发版）只在对话里说过，未落盘 → 可补进 `docs/05` 或独立段落。

## 3. 学术/工程未决

- [ ] **本体/图纸层只有笔记（docs/06）**：L1 依赖与数据流图如何推导、值域类型词汇从哪来、schema↔OWL 缝合，全未定。
- [ ] **`description` 四节无机器 lint**：靠维护者人工打回。可加"标题齐全性"软警告。
- [ ] **`verified` 生命周期未定义**：谁复核？升级版本要不要重验？verified 原子如何撤回？（拍板项 §1 联动）
- [ ] **DSH developer preview 破坏性变更**：peer 锁 rc 版本；跨版本维护策略要定期审视。
- [ ] **"创造性在组合/框架层、框架也原子化"是主张未验证**：对应 docs/04 的 H1-H4，先当假说，勿过度宣称。

## 4. 语义口径（对外防混淆）

- 原子/能力原子/实现原子/函数级——已澄清（docs/01），对外表述用统一词防再次混淆。
- "收录 vs 收入""渐进披露 vs 图纸"——对话里反复出现过歧义，文档里尽量固定用词。

## 5. 建议的下一批研究/设计动作（按性价比）

1. 把"只收 manifest vs 拿到即 MVP"矛盾拍板 → 落成 `verified` 语义 v0.2。
2. docs/04 的 H3 微型预实验（10~20 原子两档粒度测选砖命中率），给"检索 vs 再生成"首个数据。
3. （可选）L1 依赖图推导 demo：从 `deps` + input/output 生成 Atom 依赖/数据流 JSON-Graph。
4. （可选）`description` 四节标题齐全性 lint（软警告）。
