# Contributing · 投稿一个原子

> 欢迎任何人投稿"你解决的问题"。把能力封装成一个 **Atom**，让全世界复用，而不是各自重写。

## 谁可以投稿

**任何人都可以。** 造原子的能力是少数人的，拼原子的能力是所有人的——但**封装原子**不需要你是专家：只要你能把一个输入明确、输出明确的能力写清楚，就可以投稿。

## 一个原子必须满足什么

一份 Atom manifest（JSON）至少声明四件事：

| 字段 | 要求 | 反例（为什么不行） |
| --- | --- | --- |
| `intent` | 用户能**一句话说清**的意图 | "实现发邮件功能的一串底层逻辑"——那不是一个意图 |
| `input` | 输入数据的明确形状（JSON Schema） | 不写 input，或写"任意数据" |
| `output` | 输出数据的明确形状（JSON Schema） | 不写 output |
| 原子性 | **一个**意图一个原子 | 一个原子同时"读 PDF 又发邮件"——拆成两个 |

判定尺子：*再拆就得解释"怎么做"了 → 太小；装下两个意图 → 太大。*

另外建议：给 `side_effects`（是否碰世界：网络/文件/发信）、配 `tests` 样例、说明实现语言。详见 `spec/atom.schema.json` 与 `docs/03`。

## 流程（当前草案阶段）

1. **看样例**：`atoms/` 里有几个 manifest 样例，照着写。
2. **自检**：用 `spec/atom.schema.json` 校验你的 manifest 合法（联网工具可选）。
3. **开 Issue 或 PR**：
   - 只有 manifest / 想法 → 开 **Issue**（标题建议：`[atom] <你的意图，如: 从PDF抽表格>`）；
   - 有完整实现 → 开 **PR**，把实现与 manifest 放入 `atoms/<domain>.<verb>.atom.json`。

## 许可

默认：投稿内容以 **MIT** 授权进入公共库。投稿即表示你同意该授权；如涉及第三方代码，请注明来源与许可。

## 别做什么

- 不要把**实现细节**写进对外说明——原子是黑盒。
- 不要投稿含密钥/凭据/私有数据的内容。
- 不要"顺手加选项"膨胀原子——那是 UNIX 后期每个工具都踩的坑（见 `docs/02` 缺口二）。
